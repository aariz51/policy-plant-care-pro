<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.safemama.app">  <!-- <<< MODIFIED: Added package attribute -->

    <!-- Permissions added for image picking and camera -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <!-- Add INTERNET permission if not already implicitly added by other packages (Supabase/http need it) -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Google Play Billing permission for in-app purchases and subscriptions -->
    <uses-permission android:name="com.android.vending.BILLING"/>


    <application
        android:label="safemama"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <!-- Meta-data for Flutter deep linking -->
            <meta-data android:name="flutter_deeplinking_enabled" android:value="true" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- === ADD THIS NEW INTENT FILTER FOR DEEP LINKS === -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!-- This tells Android to catch links starting with "com.safemama.app" -->
                <data android:scheme="com.safemama.app" />
            </intent-filter>

            <!-- /// START OF DEEP LINK INTENT FILTER FOR SUPABASE OAUTH CALLBACK /// -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!--
                    IMPORTANT:
                    android:scheme should match the scheme part of your redirectTo URL.
                    If your redirectTo is 'com.safemama.app://login-callback/',
                    then android:scheme is "com.safemama.app".
                    This scheme MUST match your applicationId (or a custom scheme you control).
                    Using your applicationId is common for app-specific links.

                    android:host should match the host part of your redirectTo URL.
                    If your redirectTo is 'com.safemama.app://login-callback/',
                    then android:host is "login-callback".
                -->
                <data android:scheme="com.safemama.app" android:host="login-callback" />
            </intent-filter>
            <!-- /// END OF DEEP LINK INTENT FILTER /// -->

            <!-- DODO PAYMENTS DEEP LINKING for payment-success -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="com.safemama.app" android:host="payment-success" />
            </intent-filter>
            <!-- END OF DODO PAYMENTS DEEP LINKING -->

        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- If you are using url_launcher to launch URLs, you might need to add queries for http and https -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="http" />
        </intent>
        
        <!-- /// ADDED FOR SPEECH_TO_TEXT PACKAGE /// -->
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>

    </queries>
</manifest>